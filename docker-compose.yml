services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: resume-info-api
    ports:
      - "8000:8000"
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
    restart: unless-stopped
